"""Interneto Install — a cross-platform TUI for the Interneto toolbox.

Autodetects the host operating system and package manager, lets you multi-select
packages just like the web toolbox, and actually runs the install instead of
asking you to copy/paste a command.
"""

__version__ = "0.1.0"
